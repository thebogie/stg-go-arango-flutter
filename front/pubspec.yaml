name: jwt_auth
description: Flutter JWT Auth with dio, provider, and shared_preferences
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.1.2
  provider: ^6.0.5
  shared_preferences: ^2.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter:
  uses-material-design: true
